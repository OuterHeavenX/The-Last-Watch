export class AudioManager { private ctx?:AudioContext; enabled=true; private tone(freq:number,duration=.08,volume=.025,type:OscillatorType='square'){if(!this.enabled)return;this.ctx??=new AudioContext();const o=this.ctx.createOscillator(),g=this.ctx.createGain();o.type=type;o.frequency.value=freq;g.gain.setValueAtTime(volume,this.ctx.currentTime);g.gain.exponentialRampToValueAtTime(.0001,this.ctx.currentTime+duration);o.connect(g).connect(this.ctx.destination);o.start();o.stop(this.ctx.currentTime+duration);} click(){this.tone(180,.04,.018)} attack(){this.tone(420,.035,.012)} victory(){[392,523,659].forEach((f,i)=>setTimeout(()=>this.tone(f,.22,.035,'triangle'),i*130))} defeat(){[220,174,130].forEach((f,i)=>setTimeout(()=>this.tone(f,.28,.03,'sawtooth'),i*170))} }
export const audio=new AudioManager();
